node_modules/
dist/
.next/
out/
*.tsbuildinfo
.env
.env.local
.env.*.local
.DS_Store
*.log
.turbo/
coverage/
support-bundle-*/

# Private operator notes — never commit (may contain ownership/IP context)
*.local.md

# Local dev tooling (preview launch config, etc.)
.claude/

# Local cost-capture artifacts (may contain org/usage data)
cost-capture-*/
CLAUDE.md
